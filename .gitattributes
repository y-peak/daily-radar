# 换行符统一为 LF：仓库里存 LF，工作区也保持 LF。
#
# 为什么必须显式声明 —— 这个项目在 Windows 上开发，却通过 volcano put
# 部署到 Linux。Windows 上 git 默认 core.autocrlf=true，会在下次
# checkout 时把工作区的文件改成 CRLF；那些文件再被传上服务器，
# 就会变成 `bash: $'\r': command not found` —— 而且报错点跟根因隔得很远。
# 用 eol=lf 把工作区钉在 LF，从源头消除这个隐患。
* text=auto eol=lf

# 图片按二进制处理，杜绝任何文本转换把文件改坏
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary

# 签名材料与安装包同理（本来也在 .gitignore 里，这里是双保险）
*.apk binary
*.jks binary
*.keystore binary
