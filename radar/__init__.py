"""个人情报台 —— 一个按模块聚合的每日情报应用。

设计目标（三句话）：
    1. 加一个模块 = 加一个目录。核心不认识任何具体模块。
    2. 数据流水线离线跑（定时或手动），产物是静态页面 + JSON。
    3. 线上只跑一个极简 web 服务：发静态文件 + 提供"有没有更新"的口子。

对外入口见 `__main__.py`。
"""

__version__ = "0.1.0"
