<?xml version="1.0" encoding="utf-8"?>
<!--
  个人情报台 —— 安卓壳
  ~~~~~~~~~~~~~~~~~~~
  这是一个**真的 APK**，不是网页快捷方式。

  为什么要自己做壳：小米（国行 HyperOS / MIUI）自带浏览器不支持 PWA 安装，
  只会生成一个"书签快捷方式"——打开还是浏览器，看起来就跟网页一样。
  而且国行小米普遍没有 Google Play，装不了 Chrome/Edge，所以
  "用 Chrome 打开再添加到主屏幕"这条路在用户的机器上走不通。

  壳本身很薄：一个全屏 WebView 加载线上站点。内容仍然由服务器渲染，
  所以改页面不用重新发版（和 PWA 一样的更新机制，靠指纹轮询自动 reload）。

  targetSdk 35：Android 15 起要求 targetSdk >= 24 才允许安装；
  另外 targetSdk >= 30 时 launcher activity **必须**显式写 android:exported。
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ypeak.radar">

    <!--
      versionCode / versionName 交给 build.sh 的参数给（单一来源，
      免得改了脚本里的版本号、manifest 里还是旧的）。
      注意：XML 注释里不能出现连续两个减号，所以上面那两个参数名这里就不复述了。
      uses-sdk 写在这里是为了让 manifest 自己说清底线（API 24 起）。
    -->
    <uses-sdk android:minSdkVersion="24" android:targetSdkVersion="35" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
      ⭐ REQUEST_INSTALL_PACKAGES：应用内安装新版本包所必需（API 26 起）。

      为什么现在要它：以前"立即更新"是丢给系统浏览器去下、去装 ——
      下载和安装都发生在浏览器里，壳不需要任何安装权限，用户却要
      在两个应用之间来回跳，下完还得自己找到安装包点一下。
      改成应用内下载 + 拉起系统安装器之后，就必须要这个权限了。

      ⚠️ 拿到这个权限**不等于**能静默安装。系统仍会弹一次安装确认 ——
      那是安卓对所有"非应用商店来源"的硬性要求，只有 root 或
      Device Owner（设备管理）才绕得过去，普通 sideload 应用做不到。
      所以文案里如实写了"系统会弹一次确认"，不假装零打扰。
    -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- 手机（竖屏为主），不需要任何硬件特性，别让应用商店/系统误判为不兼容 -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <!--
      规划到期提醒（原生系统通知）需要下面三个权限。

      · POST_NOTIFICATIONS —— API 33 起发通知要的**运行时权限**（要弹框问，
        不是装上就有）。API 32 及之前装在包里就等于给了，但代码里照样要查
        "系统设置里有没有被关掉"，见 Notifier.canNotify()。

      · RECEIVE_BOOT_COMPLETED —— 闹钟**不跨重启存活**。没有它，
        手机重启一次之后提醒就永久没了，而且不会报任何错（最阴的一类失效）。
        这是普通权限，装上就有，不用动态申请。

      · SCHEDULE_EXACT_ALARM —— "准点响"要的特殊权限（API 31 起）。
        ⚠️ Android 14 起**新装应用默认拿不到**，setExact* 会直接抛
        SecurityException。所以代码里必须先 canScheduleExactAlarms() 问一句，
        拿不到就退化成不精确闹钟（一定会响、但可能晚一些），
        并把这件事如实显示在设置页上 + 给一个"去开启"的入口。
        声明它不会有害：用户不给，也只是降级而已。
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:theme="@style/AppTheme"
        android:allowBackup="true"
        android:supportsRtl="false"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|smallestScreenSize|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          把下好的 APK 交给系统安装器时要给一个 URI。从 Android 7（API 24）起，
          `file://` 形式的 URI 一律被系统拒绝（FileUriExposedException），
          必须用 `content://`。

          为什么自己写一个 Provider 而不是用现成的 FileProvider：
          标准做法是 androidx.core 的 FileProvider，但它属于 AndroidX ——
          而本工程刻意**不引任何依赖库**（不跑 Gradle，整个 APK 只有我们自己
          编出来的 classes.dex + 资源）。为了一个"把文件读出去"的能力拖进
          一整个 AndroidX，不划算。框架自带的 ContentProvider 就够了。

          三个属性的作用：
            · exported=false         —— 别的应用不能随便读我们目录里的文件
            · grantUriPermissions=true —— 允许我们用 Intent 临时授权给安装器
            · 配合 Intent 上的 FLAG_GRANT_READ_URI_PERMISSION，
              安装器只在这一次 Intent 的生命周期内可读，用完自动失效
        -->
        <provider
            android:name=".ApkProvider"
            android:authorities="com.ypeak.radar.apk"
            android:exported="false"
            android:grantUriPermissions="true" />

        <!--
          规划到期提醒的接收器。两个职责：
            1. 到期闹钟响的时候发通知（action 见 PlanReminder.ACTION_DUE，
               由我们自己的 PendingIntent 显式指名送过来，不需要 intent-filter）
            2. 在所有"排好的闹钟已经失效"的时机重排 —— 开机、覆盖安装、
               改系统时间、换时区、以及"闹钟与提醒"权限刚被授予

          ⚠️ exported 这里**必须写 true**，不能想当然地用 false。
             官方 <receiver> 文档的字面意思是"false 时仍能收到系统发出的消息"，
             但社区里大量实例反馈 targetSdk 31+ 上 BOOT_COMPLETED 会因此收不到；
             而收不到的后果是静默的（重启后提醒永久失效，不报错）。
             两边的代价完全不对称，所以选"一定能收到"的那边。

             代价是别的应用也能往这里发广播。但这个接收器本身就是**幂等**的：
             它在 for 的只有"今天有没有到期未完成的规划"这一个事实 ——
             没有就什么都不做；有的话，那也正好是它本来就该发的那条通知。
             伪造成功最多让人看到一条自己应得的提醒，拿不到任何额外能力。
        -->
        <receiver
            android:name=".PlanAlarmReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED" />
            </intent-filter>
        </receiver>

        <!--
          每日简报（早间 / 尾盘 / 收盘）的接收器。和上面那个是**并列**的两个，
          不是它的一个分支：用户可能只开简报、不开规划提醒（或反过来），
          两边必须互不牵连。开机时系统会把两个都叫起来，各自重排各自的闹钟。

          ⚠️ exported 同样必须写 true（理由见上面那段）：
             收不到 BOOT_COMPLETED 的后果是"重启之后简报永久不响"，且不报错。

          ⚠️ 这个接收器**不是幂等的**（它会真的去拉一次网络并弹通知）。
             别的应用理论上能伪造 ACTION_BRIEF_DUE 让它提前响一条 ——
             但三条防线在：① 每个时段每天只发一次（已发记录挡着，in BriefAlarm）；
             ② 伪造的响应里没有 extra，slot 会退化成收盘；
             ③ 内容只能来自我们自己的服务器。最坏结果就是那天提前看到一条
             本来就该收到的简报 —— 拿不到任何额外能力。
        -->
        <receiver
            android:name=".BriefAlarmReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
